{
  "name": "jm-voyager",
  "version": "1.0.0",
  "description": "Site web JM Voyager — Agence de voyages sur mesure",
  "scripts": {
    "dev": "php -S localhost:8000",
    "start": "php -S localhost:8000",
    "open": "open http://localhost:8000/accueil.php"
  },
  "engines": {
    "node": ">=18"
  }
}
